{% extends 'base.html.twig' %}

{% block title %} {{product.name}} - La Boutique!{% endblock %}

{% block content %}
        <h1>{{product.name}}</h1>

           <div class="row">

           <div class="col-md-4">
              <img src="/uploads/{{product.illustration}}" alt="{{product.name}}" class="img-fluid">
             </div>

                <div class="col-md-8">
                       <h5>{{product.name}}</h5>
                        <h6><span>{{product.subtitle}}</span></h6>
                        <p>{{product.description}}</p>
                        <br> €{{product.price /100}}<br>
                        <div class="mt-2"><a href="" class="btn btn-primary">Ajouter au panier</a></div>
                    </div>
                    </div>
                    
       
        </div>
       
       
        
{% endblock %}
